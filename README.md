# time tracker - Chrome 拡張機能

Chrome ツールバーの拡張アイコンだけで操作する、8時間通知タイマーです。

---

## 現在の仕様

### 操作方法（ウィンドウ不要）

- 拡張アイコンを 1 回クリック: `0` で開始待ち（5秒）
- 5秒以内にもう 1 回クリック: `1`
- さらに 5秒以内クリックで `2`, `3` ... と増加
- 最大は `7`（以降は `7` のまま）
- 5秒間クリックが止まると、その数字で計測開始

例:

- `2` で開始した場合、開始時点で 2 時間経過扱い
- そのため 8 時間通知までは残り 6 時間

### 通知仕様

- 8時間ごとに約 4 秒のビープ音を再生
- 通知後も停止せず、連続して 8 時間ごとに通知を継続

### アイコン表示仕様

- 計測中の数値は 24 時間表示でループ
- 24時間ちょうどで `24` を表示し、その後 `0` に戻る
- 数字はできるだけ大きく表示されるよう自動調整

### 色

| 状態 | アイコン色 |
|------|------------|
| 未開始 | グレー |
| 数字選択中（5秒待ち） | 黄系（アンバー） |
| 計測中（8h未満） | 青 |
| 8h以上 | オレンジ |
| 10h以上 | 赤 |

---

## 手動インストール手順

1. Chrome を開き、アドレスバーに以下を入力して Enter

   ```
   chrome://extensions/
   ```

2. 右上の「デベロッパーモード」を ON
3. 「パッケージ化されていない拡張機能を読み込む」をクリック
4. このフォルダ（`time_tracker`）を選択
5. ツールバーに表示された拡張アイコンをピン留めして利用

---

## ファイル構成

```
time_tracker/
├── manifest.json
├── background.js
├── offscreen.html
├── offscreen.js
├── popup.html
├── popup.js
└── README.md
```

注: 現在の操作はアイコンクリックのみで、ポップアップ画面は使用しません。

---

## 動作要件

- Chrome 116 以上（Offscreen API 対応）
